---
layout: default
title: Home
permalink: /table
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blackjack</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
    }

    #game-container {
      margin: 50px auto;
      max-width: 600px;
    }

    #dealer-hand,
    #player-hand {
      margin-bottom: 20px;
    }

    #dealer-cards,
    #player-cards {
      display: flex;
      justify-content: center;
    }

    .card {
      border: 1px solid black;
      padding: 10px;
      margin: 0 5px;
      background-color: white;
      color: black;
      border-radius: 5px;
      width: 50px;
      height: 80px;
      line-height: 80px;
      font-size: 16px;
    }

    .hidden-card {
      background-color: gray;
      color: white;
    }

    .chip-container {
      margin-bottom: 20px;
      position: relative;
    }

    .chip-button {
      display: inline-block;
      width: 50px;
      height: 50px;
      border-radius: 25px;
      background-color: #007bff;
      color: white;
      font-size: 18px;
      line-height: 50px;
      margin: 0 10px;
      cursor: pointer;
    }

    #bet-chips {
      display: inline-block;
      width: 50px;
      height: 50px;
      border-radius: 25px;
      background-color: #28a745;
      color: white;
      font-size: 18px;
      line-height: 50px;
      margin-right: 10px;
    }

    #user-info {
      position: absolute;
      top: 0;
      right: 0;
      padding: 10px;
      background-color: #f0f0f0;
      border: 1px solid #ddd;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <div id="user-info">
    <span id="username"></span><br>
    <span id="chips"></span>
  </div>
  <div id="game-container">
    <div id="dealer-hand">
      <h2>Dealer's Hand</h2>
      <div id="dealer-cards"></div>
    </div>
    <div id="player-hand">
      <h2>Player's Hand</h2>
      <div id="player-cards"></div>
    </div>
    <div class="chip-container">
      <button class="chip-button" onclick="addChips(100)">100</button>
      <button class="chip-button" onclick="addChips(250)">250</button>
      <button class="chip-button" onclick="addChips(500)">500</button>
      <button class="chip-button" onclick="addChips(1000)">1000</button>
      <div id="bet-chips">0</div>
    </div>
    <button onclick="startGame()">Deal</button>
  </div>

  <script>
    const dealerHandElement = document.getElementById('dealer-cards');
    const playerHandElement = document.getElementById('player-cards');
    const usernameElement = document.getElementById('username');
    const chipsElement = document.getElementById('chips');
    let betChips = 0;

    // Retrieve user info from local storage
    const username = localStorage.getItem('username');
    const chips = localStorage.getItem('chips');

    // Display user info
    usernameElement.textContent = `Username: ${username}`;
    chipsElement.textContent = `Chips: ${chips}`;

    function addChips(amount) {
      betChips += amount;
      document.getElementById('bet-chips').innerText = betChips;
    }

    function startGame() {
      // Clear previous cards
      dealerHandElement.innerHTML = '';
      playerHandElement.innerHTML = '';

      // Reset bet chips
      betChips = 0;
      document.getElementById('bet-chips').innerText = betChips;

      // TODO: Implement game logic here
      // For demonstration purpose, adding example cards
      dealerHandElement.innerHTML = `
        <div class="card hidden-card">?</div>
        <div class="card">K</div>
      `;
      playerHandElement.innerHTML = `
        <div class="card">7</div>
        <div class="card">A</div>
      `;
    }
  </script>
</body>
</html>
